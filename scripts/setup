#!/usr/bin/env bash
set -e
cd "$(dirname "$0")/.."

# Install into the project venv regardless of what's on PATH
export PATH="$PWD/.venv/bin:$PATH"

pip install -r requirements.txt -r requirements_test.txt